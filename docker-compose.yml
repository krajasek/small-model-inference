services:
  inference:
    build:
      context: .
      dockerfile: Dockerfile
    image: small-model-inference:latest
    container_name: inference-server
    ports:
      - "${INFERENCE_PORT:-8000}:8000"
    volumes:
      # Mount model directory from host
      # Set MODEL_PATH environment variable to your model location
      - ${MODEL_PATH:-./models}:/models:ro
    environment:
      # Model configuration
      - INFERENCE_MODEL_PATH=/models
      # INFERENCE_MODEL_NAME - only set if you want to override the model name (optional)
      # Server configuration
      - INFERENCE_HOST=0.0.0.0
      - INFERENCE_PORT=8000
      # Device configuration
      - INFERENCE_DEVICE=${INFERENCE_DEVICE:-cpu}
      - INFERENCE_NUM_THREADS=${INFERENCE_NUM_THREADS:-4}
      # Optimization settings
      - INFERENCE_QUANTIZATION=${INFERENCE_QUANTIZATION:-none}
      - INFERENCE_ENABLE_TORCH_COMPILE=${INFERENCE_ENABLE_TORCH_COMPILE:-false}
      # Memory settings
      - INFERENCE_LOW_CPU_MEM_USAGE=${INFERENCE_LOW_CPU_MEM_USAGE:-true}
      # INFERENCE_MAX_MEMORY_GB - only set if you need a memory limit (optional)
      # Generation settings
      - INFERENCE_MAX_SEQUENCE_LENGTH=${INFERENCE_MAX_SEQUENCE_LENGTH:-2048}
      - INFERENCE_MAX_NEW_TOKENS=${INFERENCE_MAX_NEW_TOKENS:-256}
      # Caching settings (all enabled by default for performance)
      - INFERENCE_ENABLE_RESPONSE_CACHE=${INFERENCE_ENABLE_RESPONSE_CACHE:-true}
      - INFERENCE_ENABLE_PROMPT_CACHE=${INFERENCE_ENABLE_PROMPT_CACHE:-true}
      - INFERENCE_ENABLE_TOKENIZER_CACHE=${INFERENCE_ENABLE_TOKENIZER_CACHE:-true}
      - INFERENCE_USE_KV_CACHE=${INFERENCE_USE_KV_CACHE:-true}
      # Batching (disabled by default, enable for high-throughput)
      - INFERENCE_ENABLE_BATCHING=${INFERENCE_ENABLE_BATCHING:-false}
      - INFERENCE_MAX_BATCH_SIZE=${INFERENCE_MAX_BATCH_SIZE:-8}
      # Observability (disabled by default)
      - INFERENCE_ENABLE_TRACING=${INFERENCE_ENABLE_TRACING:-false}
      - INFERENCE_LANGFUSE_PUBLIC_KEY=${INFERENCE_LANGFUSE_PUBLIC_KEY:-}
      - INFERENCE_LANGFUSE_SECRET_KEY=${INFERENCE_LANGFUSE_SECRET_KEY:-}
      - INFERENCE_LANGFUSE_HOST=${INFERENCE_LANGFUSE_HOST:-}
    restart: unless-stopped
    # Resource limits (adjust based on your model size)
    deploy:
      resources:
        limits:
          memory: ${MEMORY_LIMIT:-8G}
        reservations:
          memory: ${MEMORY_RESERVATION:-4G}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
